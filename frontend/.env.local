# Configuración para desarrollo local
# El proxy de Vite redirigirá las peticiones al backend

# Dejar vacío para usar el proxy de Vite
VITE_API_BASE_URL=

# URL del backend local (ajusta el puerto si es necesario)
VITE_BACKEND_URL=http://localhost:5000
